// import { Test } from '@nestjs/testing';
// import { BackendProductsApiService } from './backend-products-api.service';

describe('BackendProductsApiService', () => {
  // let service: BackendProductsApiService;
  //
  // beforeEach(async () => {
  //   const module = await Test.createTestingModule({
  //     providers: [BackendProductsApiService],
  //   }).compile();
  //
  //   service = module.get(BackendProductsApiService);
  // });

  it('should be defined', () => {
    // expect(service).toBeTruthy();
    expect(1).toBeTruthy();
  });
});
