// import { HttpExceptionFilter } from './backend-filters';

describe('backendFilters', () => {
  it('should work', () => {
    expect(1).toEqual(1);
    // expect(backendFilters()).toEqual('backend-filters');
  });
});
