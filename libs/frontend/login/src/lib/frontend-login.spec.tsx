// import { render } from '@testing-library/react';
//
// import FrontendLogin from './frontend-login';

describe('FrontendLogin', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<FrontendLogin />);
    // expect(baseElement).toBeTruthy();
    expect(1).toBeTruthy();
  });
});
